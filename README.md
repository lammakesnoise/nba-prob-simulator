# NBA Seeding Probability Simulator

Monte Carlo simulation of the NBA regular season where every remaining game is a 50/50 coin flip. Displays seed probabilities as a heatmap for each conference.

## Setup

```bash
pip install -r requirements.txt
python app.py
```

Open http://localhost:8080
